@model IEnumerable<Entity.Product>


<table class="table table-striped">
    @*<tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.price)
            </th>

            <th></th>
        </tr>*@

    @foreach (var item in (List<Entity.Product>)Session["cart"])
    {
        <tr>
            <td>

                @Html.DisplayFor(modelItem => item.name)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.ourPrice)
            </td>

            <td>
                <span class="glyphicons glyphicons-remove">
                    @Html.ActionLink("Remove", "Edit", "Order", new { id = item.id, businessId = item.businessId }, new { @class = "btn btn-danger btn-sm" })
                </span>
            </td>
        </tr>
    }

</table>
