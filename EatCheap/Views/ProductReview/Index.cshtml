@model IEnumerable<Entity.ProductReview>

@{
    ViewBag.Title = "Index";
}

<h2>Reviews</h2>


<table class="table  table-striped table-bordered " style="width:45%; margin: 50px auto;">
    <tr>
        @*<th>
                    @Html.DisplayNameFor(model => model.Review)
                </th>

                <th></th>
            </tr>*@

        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Review)
            </td>

            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>*@
        </tr>
        }

    </table>

<p>
    @Html.ActionLink("Post a Review", "Create", "ProductReview", new { productId = @ViewBag.ProductId, businessId = @ViewBag.BusinessId }, new { @class = "btn btn-sm btn-primary" })
    @Html.ActionLink("Back to Menu", "Index", "UserProduct", new { id = @ViewBag.BusinessId }, new { @class = "btn btn-sm btn-primary" })
</p>